version: "3.8"

services:
  detectionengine-matcher:
    container_name: detectionengine-matcher

    build:
      context: ../../..
      dockerfile: docker/Dockerfile

    restart: unless-stopped

    ports:
      - "7003:7003"

    env_file:
      - .env
