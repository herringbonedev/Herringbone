[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "herringbone-modules"
version = "0.1.0"
description = "Shared auth and database modules for Herringbone"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
