secrets:
  jwt_secret:
    file: ./jwt_secret.txt
  service_jwt_private_key:
    file: ./service_jwt_private.pem
  service_jwt_public_key:
    file: ./service_jwt_public.pem
services:
  herringbone-auth:
    container_name: herringbone-auth
    secrets:
      - jwt_secret
      - service_jwt_private_key
      - service_jwt_public_key
    image: quay.io/herringbone/herringbone-auth:3cbddddd@sha256:e380b233090b8d7a51f91439b7d5adc6f280fcffde69430b98cd1ee4f0c5fb67
    restart: unless-stopped
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - herringbone
    ports:
      - "7001:7001"
    environment:
      MONGO_HOST: ${MONGO_HOST}
      MONGO_PORT: ${MONGO_PORT}
      MONGO_USER: ${MONGO_USER}
      MONGO_PASS: ${MONGO_PASS}
      DB_NAME: ${DB_NAME}
      AUTH_DB: ${AUTH_DB}
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
networks:
  herringbone:
    external: true
