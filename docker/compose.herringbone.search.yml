services:
  herringbone-search:
    image: quay.io/herringbone/herringbone-search:ec5eb832@sha256:ee656c750c5f183465a1a8d80d4df1558b76da36d109d4e1477d9b8719c98fff
    restart: unless-stopped
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - herringbone
    expose:
      - "7014"
    environment:
      MONGO_HOST: ${MONGO_HOST}
      MONGO_PORT: ${MONGO_PORT}
      MONGO_USER: ${MONGO_USER}
      MONGO_PASS: ${MONGO_PASS}
      DB_NAME: ${DB_NAME}
      AUTH_DB: ${AUTH_DB}
      HERRINGBONE_SERVICE: herringbone-search
      HERRINGBONE_UNIT: herringbone
      HERRINGBONE_ELEMENT: search
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    secrets:
      - jwt_secret
    deploy:
      replicas: ${HERRINGBONE_SEARCH_REPLICAS:-3}
networks:
  herringbone:
    external: true
    name: herringbone_net
secrets:
  jwt_secret:
    file: ./secrets/runtime/jwt_secret
