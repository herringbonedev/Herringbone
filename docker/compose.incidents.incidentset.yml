services:
  incidents-incidentset:
    image: quay.io/herringbone/incidents-incidentset:ef5d46b4@sha256:407744cdc0a2af712dda7d03b2cfabaa4b9888de92ea99da278cb596cd8c136d
    restart: unless-stopped
    networks:
    - herringbone
    expose:
    - "7011"
    environment:
      MONGO_HOST: ${MONGO_HOST}
      MONGO_PORT: ${MONGO_PORT}
      MONGO_USER: ${MONGO_USER}
      MONGO_PASS: ${MONGO_PASS}
      DB_NAME: ${DB_NAME}
      AUTH_DB: ${AUTH_DB}
      COLLECTION_NAME: incidents
      HERRINGBONE_SERVICE: incidents-incidentset
      HERRINGBONE_UNIT: incidents
      HERRINGBONE_ELEMENT: incidentset
    read_only: true
    tmpfs:
    - /tmp
    security_opt:
    - no-new-privileges:true
    secrets:
    - service_token
    - service_jwt_public_key
    - jwt_secret
    deploy:
      replicas: ${INCIDENTS_INCIDENTSET_REPLICAS:-3}
secrets:
  service_token:
    file: ./secrets/runtime/incidents_incidentset_service_token
  service_jwt_public_key:
    file: ./secrets/runtime/service_jwt_public_key
  jwt_secret:
    file: ./secrets/runtime/jwt_secret
networks:
  herringbone:
    external: true
    name: herringbone_net
