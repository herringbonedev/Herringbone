apiVersion: apps/v1
kind: Deployment
metadata:
  name: herringbone-receiver-remote
  namespace: herringbone
spec:
  replicas: 1
  selector:
    matchLabels:
      app: herringbone-receiver-remote
  template:
    metadata:
      labels:
        app: herringbone-receiver-remote
    spec:
      containers:
        - name: receiver
          image: quay.io/herringbone/receiver
          ports:
            - containerPort: 7004
              name: tcp-port
              protocol: TCP
          env:
            - name: RECEIVER_TYPE
              value: REMOTE
            - name: MONGO_HOST
              value: mongodb-headless.mongodb.svc.cluster.local   # no port here
            - name: MONGO_PORT
              value: "27017"
            - name: DB_NAME
              value: herringbone
            - name: COLLECTION_NAME
              value: logs
            # Optional (set to your real values):
#            - name: MONGO_AUTH_SOURCE
#              value: admin
            - name: MONGO_REPLICA_SET
              value: rs0            # or leave unset for standalone
            - name: MONGO_USER
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: username
            - name: MONGO_PASS
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: password
