apiVersion: v1
kind: Service
metadata:
  name: herringbone-cardset-service
  namespace: herringbone
spec:
  type: ClusterIP
  selector:
    app: herringbone-cardset
  ports:
    - name: tcp
      protocol: TCP
      port: 7002
      targetPort: 7002