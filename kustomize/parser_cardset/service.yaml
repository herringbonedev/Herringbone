apiVersion: v1
kind: Service
metadata:
  name: parser-cardset-service
  namespace: herringbone
spec:
  type: LoadBalancer
  selector:
    app: parser-cardset
  ports:
    - name: http
      protocol: TCP
      port: 7005
      targetPort: 7005
