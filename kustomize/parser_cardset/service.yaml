apiVersion: v1
kind: Service
metadata:
  name: parser-cardset-service
  namespace: herringbone
spec:
  type: LoadBalancer     # <-- exposes service externally
  selector:
    app: parser-cardset
  ports:
    - name: http
      protocol: TCP
      port: 7002          # external port
      targetPort: 7002    # container port
