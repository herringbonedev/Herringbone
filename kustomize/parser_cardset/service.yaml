apiVersion: v1
kind: Service
metadata:
  name: parser-cardset-service
  namespace: herringbone
spec:
  type: ClusterIP
  selector:
    app: parser-cardset
  ports:
    - name: tcp
      protocol: TCP
      port: 7002
      targetPort: 7002