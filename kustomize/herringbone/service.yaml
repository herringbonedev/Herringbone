apiVersion: v1
kind: Service
metadata:
  name: herringbone-app-service
  namespace: herringbone
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: herringbone-app
  ports:
    - name: tcp
      protocol: TCP
      port: 7002
      targetPort: 7002