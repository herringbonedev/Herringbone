apiVersion: v1
kind: Service
metadata:
  name: herringbone-mind-recon-service
  namespace: herringbone
spec:
  type: ClusterIP
  selector:
    app: herringbone-mind-recon
  ports:
    - name: tcp
      protocol: TCP
      port: 8001
      targetPort: 8001