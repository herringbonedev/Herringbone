apiVersion: v1
kind: Service
metadata:
  name: parser-extractor-service
  namespace: herringbone
spec:
  type: LoadBalancer     # <-- exposes service externally
  selector:
    app: parser-extractor
  ports:
    - name: http
      protocol: TCP
      port: 7006          # external port
      targetPort: 7006    # container port