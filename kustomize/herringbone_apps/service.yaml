apiVersion: v1
kind: Service
metadata:
  name: herringbone-apps-service
  namespace: herringbone
spec:
  type: ClusterIP
  selector:
    app: herringbone-apps
  ports:
    - name: tcp
      protocol: TCP
      port: 7002
      targetPort: 7002