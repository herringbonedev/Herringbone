apiVersion: v1
kind: Service
metadata:
  name: detectionengine-ruleset-service
  namespace: herringbone
spec:
  type: LoadBalancer
  selector:
    app: detectionengine-ruleset
  ports:
    - name: http
      protocol: TCP
      port: 7002
      targetPort: 7002