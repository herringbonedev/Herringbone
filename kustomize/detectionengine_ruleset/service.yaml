apiVersion: v1
kind: Service
metadata:
  name: detectionengine-ruleset-service
  namespace: herringbone
spec:
  type: ClusterIP
  selector:
    app: detectionengine-ruleset
  ports:
    - name: tcp
      protocol: TCP
      port: 7002
      targetPort: 7002