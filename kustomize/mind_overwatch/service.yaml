apiVersion: v1
kind: Service
metadata:
  name: herringbone-mind-overwatch-service
  namespace: herringbone
spec:
  type: ClusterIP
  selector:
    app: herringbone-mind-overwatch
  ports:
    - name: tcp
      protocol: TCP
      port: 8002
      targetPort: 8002