apiVersion: apps/v1
kind: Deployment
metadata:
  name: herringbone-mind-overwatch
  namespace: herringbone
spec:
  replicas: 1
  selector:
    matchLabels:
      app: herringbone-mind-overwatch
  template:
    metadata:
      labels:
        app: herringbone-mind-overwatch
    spec:
      containers:
        - name: mind-overwatch
          image: quay.io/herringbone/mind_overwatch
          env:
            - name: OLLAMA_MODEL
              value: "mattw/loganalyzer"
          args:
            - >
              ollama serve & sleep 10 &&
              ollama pull ${OLLAMA_MODEL} &&
              python3 overwatch.py
          ports:
            - containerPort: 8002
              name: tcp-port
              protocol: TCP
      nodeSelector:
        mind: "overwatch"
      tolerations:
        - key: "mind"
          operator: "Equal"
          value: "overwatch"
          effect: "NoSchedule"