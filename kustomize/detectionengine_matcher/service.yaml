apiVersion: v1
kind: Service
metadata:
  name: detectionengine-matcher-service
  namespace: herringbone
spec:
  type: ClusterIP
  selector:
    app: detectionengine-matcher
  ports:
    - name: tcp
      protocol: TCP
      port: 7003
      targetPort: 7003