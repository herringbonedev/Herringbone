apiVersion: v1
kind: Service
metadata:
  name: incidents-incidentset-service
  namespace: herringbone
spec:
  type: LoadBalancer
  selector:
    app: incidents-incidentset
  ports:
    - name: http
      protocol: TCP
      port: 7011
      targetPort: 7011